# Software Defined Networks Federated Anomaly Detection Tool
# Install with bash: pip install -r requirements.txt

# Tool 1 Set up
# Libraries needed
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# Model serialization to save ML model
joblib>=1.3

# Config parsing - to rerun experiment
pyyaml>=6.0

# Libraries for plotting and visualization
matplotlib>=3.7
seaborn>=0.12

# Tool 2 additions
pytest>=7.4.0  # for running tests/test_sanitizer.py
webob>=1.8.7  # required by Ryu REST API
 
# Optional: Ryu + Mininet (Linux only — install via install.sh or apt)
# ryu>=4.34
# mininet (install via: sudo apt-get install mininet)

# requirements.txt -> SDN FlowMod Injection Lab (Tool 3)
# Install with:  pip3 install -r requirements.txt

# Packet capture and crafting (Phase 1 sniffer) 
# I hardcode this with this version since I use Python 3.8. It is compat.
scapy==2.5.0

# Tool 4 add on
flask>=3.0  # Web server for the HITL operator dashboard, i.e., dashboard/app.py
flask-cors>=4.0  # CORS headers so the dashboard HTML can call the API cross origin
